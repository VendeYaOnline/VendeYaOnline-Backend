'use strict';

/**
 * suscripciones-cancelada router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::suscripciones-cancelada.suscripciones-cancelada');
