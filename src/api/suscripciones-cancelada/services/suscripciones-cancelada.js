'use strict';

/**
 * suscripciones-cancelada service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::suscripciones-cancelada.suscripciones-cancelada');
